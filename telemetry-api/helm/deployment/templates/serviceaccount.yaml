apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "telemetry-api.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "telemetry-api.labels" . | nindent 4 }}
