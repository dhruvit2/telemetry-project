apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "telemetry-collector.serviceAccountName" . }}
  labels:
    {{- include "telemetry-collector.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
